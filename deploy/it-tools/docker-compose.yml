services:
  it-tools:
    build: .
    image: pixel-home/it-tools:zh
    container_name: it-tools
    restart: unless-stopped
    ports:
      - "127.0.0.1:8788:80"
    # 纯前端工具箱，无需额外 volume / 数据库
    # 默认中文（见 Dockerfile）；右上角仍可切换语言
